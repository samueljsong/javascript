/**
 * Variable: named storage
 *  - name must contain only letters, numbers, $ and _
 *      - cannot start with a number
 *      - cannot use hyphen
 *      - no reserved words
 */
let name = "Samuel";

/**
 * Constants: unchanging variable
 *  - use all upper case and underscores
 */

const FIRST_NAME = "Samuel";
